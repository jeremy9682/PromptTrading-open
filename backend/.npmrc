# Resolve peer dependency conflict between @privy-io/server-auth (ethers v6)
# and @polymarket packages (ethers v5). The ethers v6 peer is optional in
# @privy-io/server-auth, so ignoring it here is safe.
legacy-peer-deps=true
